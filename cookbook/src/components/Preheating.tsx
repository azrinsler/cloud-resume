import * as React from "react";

import '../css/preheating.css'

const Preheating: () => React.JSX.Element = () => {
    return (
        <div id="preheating" className='flex-column' style={{width:'100%',placeContent:'center',placeItems:'center',flexGrow:'1'}}>
            <h1 id="preheating-text">Preheating</h1>
        </div>
    )
}
export default Preheating